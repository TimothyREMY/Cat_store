
  <div class="row single-product mt-5">
    <div class="col-md-6 offset-md-2">
      <div class="row">
            <div class="col-md-5 margin-b-30">
                <div class="item">
                      <%= image_tag @items.image_url, alt: 'photo de chat', class: 'w-100' %>
                </div>
            </div>
            <div class="col-md-7">
                <div class="mx-5">
                    <h3 class="title pt-2"> <%= @items.title %></h3>
                    <span class="price"> <%= @items.price %> €</span>
                    <p class="mt-2"><%= @items.description %></p>
                    <div class="available mt-5">En stock</div>
                    <div class="add-buttons">
                        <%= link_to "Ajouter au panier", item_selections_path(@items), class:"btn btn-info", method:"POST" %>
                    </div>
                </div>
            </div>
      </div>
    </div>
 

<section class="container p-3">
<hr>
  <div class="container">
  <h2 class="text-center py-3">Produits similaires</h2>
    <div class="row justify-content-center">
      <div class="card-group">
      <% @rand_items.each do |rand_item|%>
        <div class="col mx-5">
          <div class="card" style="18rem;">
            <img src="<%=rand_item.image_url%>" class="card-img-top" alt="Chaton mignon">
            <div class="card-body">
              <h4 class="card-title"><%= rand_item.title %></h4>
              <p class="card-text"><%= rand_item.description %></p>
            </div>
          </div>
        </div>
        
        <% end %>
        

        
      </div>
    </div>
  </div>
</section>    
