<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profil</title>
</head>
<body>

  <div class="container col-12 pt-5">
    <div class="row">
      <div class="col-lg-3 col-md-4 col-12">
        <div class="card border-0 mb-4">
          <div class="card-body">
            <h3>Profil</h3>
            <p> <%= @users.email %> </p>
            <%= link_to "Mettre à jour son profil", edit_user_registration_path, class:"btn btn-info" %>
          </div>
        </div>
        <div class="card border-0 mb-4">
          <div class="card-body">
            <div class="d-flex align-items-center justify-content-between border-bottom pb-3 mb-3">
              <div>
                <h5 class="mb-0 fw-bold"><%= @orders.count %></h5>
                <p class="mb-0">Commandes passées</p>
              </div>
            </div>
          </div>
        </div>
      </div>
      <div class="col-lg-9 col-md-8 col-12">
        <div class="card p-3 border-0">
          <div class="card-body">
            <h4>Historique des commandes</h4>
          </div>
            <% @orders.each do |order| %> 
      
            <div class="row row-cols-1 row-cols-md-2 g-4">
              <div class="col">

                <div class="card m-3 bg-white text-dark" style="width: 18rem;">
                    <div class="card-header">
                      Commande n°<%= order.id%> // <%= order.created_at.strftime("%Y-%d-%m") %>
                    </div>
                  <div class="card-body text-center">
                    
                                    
                    <% order.selections.each do |selection| %>
                    <p>1 photo de <%= selection.item.title %> - <%= selection.item.price %> €
                    
                    <%= link_to "Accéder à la page de la photo", item_path(selection.item.id), class:"btn btn-warning btn-sm" %></p>
                    
                    <% end %>
                    
                                
                  </div>
                  <div class="card-footer">
                    Prix total : <%= order.total_order %> €
                  </div>
                </div>
              </div>
            </div>
            
          <% end %>
        </div>
      </div>
    </div>
  </div>
  
</body>
</html>
